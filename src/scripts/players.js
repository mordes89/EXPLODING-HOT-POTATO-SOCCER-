export default class Player {

   constructor(x, y, w, h, frameX, frameY, steps) {
      this.x = x;
      this.y = y;
      this.w = w;
      this.h = h;
      this.FrameX = frameX;
      this.FrameY = frameY;
      this.steps = steps;
   }
    
}